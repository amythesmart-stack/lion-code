#!/usr/bin/env bash
# Prevent accidental direct commits to main — always work in a feature branch.
branch="$(git rev-parse --abbrev-ref HEAD)"
if [ "$branch" = "main" ]; then
  echo "Commit to a feature branch, not main directly."
  exit 1
fi

# Lint only the staged files.
npx lint-staged
